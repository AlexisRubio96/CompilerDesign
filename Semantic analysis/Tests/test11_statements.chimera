var x: integer;
    ul: list of integer;
program
	ul[0] := x;
	min();
	maximum(0, 1, nums[2]);
	loop
	    exit;
	end;
	for x in nums do println("num");  println(x); end;
	return 3 + getList()[sum(2, 3) - 2] * 2;
end;              
