const ul := {2,1,3};
var 
    x: integer;
    y: boolean;
procedure proc();
begin end;
program
	ul[0] := x;
	proc();
	loop
	    proc();
	    exit;
	end;
	for x in ul do exit; end;
	return 3 + getList()[sum(2, 3) - 2] * 2;
end;              
